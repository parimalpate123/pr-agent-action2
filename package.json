{
  "name": "pr-agent",
  "version": "0.0.1",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "npx tsc -p tsconfig.json && npx cpx \"src/prompt/*.md\" dist/src/prompt",
    "lint": "echo 'no linter yet'",
    "test": "bun test",
    "smoke:anthropic": "bun run scripts/anthropic-smoke.ts 'Hello from Anthropic'",
    "smoke:openai": "bun run scripts/openai-smoke.ts 'Hello from OpenAI'"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^5.1.1",
    "@octokit/rest": "^20.0.0",
    "node-fetch": "^3.3.0",
    "openai": "^4.25.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "cpx": "^1.5.0",
    "typescript": "^5.4.5"
  }
}
